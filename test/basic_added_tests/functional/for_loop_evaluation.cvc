extern void printInt(int val);
extern void printNewlines(int val);

int a = 0;

int foo() {
    a = a + 1;
    return 5;
}

export int main() {
    for (int i = 0, 10) {
        printInt(0);
        a = a + 1;
    }

    // TODO: after testing revert back to this
    // for (int i = 0, foo()) {
    //     printInt(0);
    //     printNewlines(1);
    // }

    // printInt(a);
    // printNewlines(1);

    // if (a != 1) {
    //     printInt(a);
    //     printNewlines(1);
    //     return 1;
    // }

    return 0;
}
