extern void printInt(int val);
extern void printSpaces(int num);
extern void printNewlines(int num);

export int main() {
    int x = 5;
    int y = 10;

    // BinOp
    int z = (2 * x) + (y / 42);

    return 0;
}